import React from 'react';
import PropTypes from 'prop-types';

ListingFavorites.propTypes = {
    
};

function ListingFavorites(props) {
    return (
        <div>
            
        </div>
    );
}

export default ListingFavorites;